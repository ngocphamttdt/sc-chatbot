# Shared secret between chatbot-api and client-server.
# Mọi request tới client-server phải kèm header `X-API-Key` = giá trị này.
INTERNAL_API_KEY=dev-key-change-me

# HTTP port (default 8001)
PORT=8001
